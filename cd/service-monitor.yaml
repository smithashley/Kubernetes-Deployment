apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    name: tea-store-app-service-monitor
    namespace: staging
spec:
  endpoints:
  - interval: 30s
    port: http
  selector:
    matchLabels:
      app: tea-store-app
