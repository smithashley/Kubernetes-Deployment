apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-tea-store
  namespace: staging
  labels:
    app: my-tea-store
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-tea-store
  template:
    metadata:
      annotations:
        prometheus.io/path: /metrics
        prometheus.io/port: "8080"
        prometheus.io/scrape: "true"
      labels:
        app: my-tea-store
    spec:
      containers:
        - name: my-tea-store
          securityContext:
            capabilities:
              drop:
              - ALL
            allowPrivilegeEscalation: false
          image: public.ecr.aws/d8i9k4x3/fso-lab-teastore-loadgen:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
