apiVersion: apps/v1
kind: Deployment
metadata:
  name: tea-store
  namespace: staging
  labels:
    app: tea-store
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tea-store
  template:
    metadata:
      annotations:
        prometheus.io/path: /metrics
        prometheus.io/port: "8080"
        prometheus.io/scrape: "true"
      labels:
        app: tea-store
    spec:
      containers:
        - name: tea-store
          securityContext:
            capabilities:
              drop:
              - ALL
            allowPrivilegeEscalation: false
          image: public.ecr.aws/eks-anywhere/hello-eks-anywhere:0.1.2-c014c0e09013bc7dcea65cc982712946d50ce582
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
